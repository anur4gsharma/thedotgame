# Client (Vite)
# Production: use the public wss:// endpoint of the dedicated realtime service.
VITE_WS_URL=wss://realtime.example.com
# Development fallback only when VITE_WS_URL is empty.
VITE_WS_PORT=3001

# Realtime service (Render/Fly/Railway/etc.)
PORT=3001
HOST=0.0.0.0
NODE_ENV=development
